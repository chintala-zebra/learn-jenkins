def myscripts
def env_params
pipeline {
    agent { 
        node {
            label 'ansible'
        }
    }
    options {
        ansiColor('xterm')
    }
    stages {
        stage('Init') {  
            steps {
                script{
                    env_params = load "pipelines/libraries/env_params.groovy"
                    myscripts = load "pipelines/upload.groovy"
                }
            }
        }
        stage('Setup Params') {  
            steps {
               script {
                    env_params.addInventoryParamsUptoHost()
                    myscripts.setupParams()
               }
            }
        }
         stage('Show Conent') {
            steps {
                script{
                    myscripts.showContent()
                }
            }
        }
        stage('copy file') {
            environment {
                ANSIBLE_VALUT= credentials('MID_ANSIBLE_VAULT')
            }
            when {
                 not { $params.target_file_path '' } 
            }
            steps {
                echo "1 ${target_file_path}"
                echo "2 ${params.target_file_path}"
                script{
                    myscripts.copyFile()
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}

