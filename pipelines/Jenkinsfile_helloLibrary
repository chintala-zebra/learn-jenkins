@Library('pipeline-library-demo') _

pipeline {
    agent any
    stages {
        stage('Print the Values') {
            steps {
                script {
                    log.info 'Starting'
                    sayHello 'Dave'
                    log.warning 'Nothing'
                }
            }
        }
    }
}