@Library('pipeline-library-demo') _

pipeline {
    agent any
    stages {
        stage('Print the Values') {
            steps {
                script {
                    sayHello 'Dave'
                }
            }
        }
    }
}